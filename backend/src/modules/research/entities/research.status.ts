export enum ResearchStatus {
  CREATED = 'created',
  UPLOADING = 'uploading',
  UPLOADED = 'uploaded',
  GENERATING = 'generating',
  GENERATED = 'generated',
  IN_MARKUP = 'in_markup',
  MARKUP_DONE = 'markup_done',
}
